<%- include("header.ejs") %>

<h1>Reading Page</h1>

<% if (data.length === 0) { %>
    <p>No posts found.</p>
<% } else { %>
    <% data.forEach((post) => { %>
        <a href="/posts/<%= post.slug %>"><%= post.title %></a>
    <% }) %>
<% } %>

<%- include("footer.ejs") %>